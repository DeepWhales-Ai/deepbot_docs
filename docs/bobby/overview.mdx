---
title: "What is Bobby"
sidebar_label: "What is Bobby"
---

{/* Source: pages-rest.jsx -> PageWhatBobby */}

import { PageHead, Para, H2, Lime, KeyValueGrid } from '@site/src/components/primitives.jsx';

<PageHead
  eyebrow="BOBBY"
  title="What is Bobby"
  lede={<>Bobby is the project facing product of DeepBot. <Lime>The intelligence network crypto TG runs on.</Lime></>}
/>

<H2 id="does">What Bobby does for a project</H2>
<Para>
  Bobby lives in project Telegram groups. It runs the chat surfaces, the trending lists, the
  ad rails, and the project side intelligence loop. For a project owner, Bobby is the
  difference between hoping a community is healthy and seeing what the community actually is.
</Para>

<H2 id="trust">The trust principle</H2>
<div className="db-faded-box">
  <div className="db-faded-box__line">We are not a billboard. We are <Lime>intelligence.</Lime></div>
</div>
<Para>
  Bobby's value is its restraint. Five ad surfaces, locked maximum. Trending earned, not paid.
  The network's outputs are calibrated against real activity, not paid placement. A project
  that qualifies for Bobby's surfaces qualifies on the merits.
</Para>

<H2 id="qualifies">What qualifies</H2>
<KeyValueGrid items={[
  { k: "Real community activity", v: "Sustained, multi source community signal. Not bot farms. Not paid pumpers." },
  { k: "Clean contract", v: "No honeypot signals. No rugged dev history on the deployer." },
  { k: "Working product", v: "Something users actually use. The bar is light at v1, but the bar exists." },
]} />
