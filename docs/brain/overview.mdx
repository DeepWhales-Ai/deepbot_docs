---
title: "Six layers, one brain"
sidebar_label: "Six layers, one brain"
---

{/* Source: pages-brain.jsx -> PageBrainOverview */}

import { PageHead, Para, H2, Lime, TaglineStrip } from '@site/src/components/primitives.jsx';
import { BrainHero, BrainStack } from '@site/src/components/BrainComponents.jsx';

<PageHead
  eyebrow="THE BRAIN"
  title={<>Six layers. <Lime>One brain.</Lime></>}
  lede="DeepBot is a signal infrastructure company with a trading bot as its monetization layer. The infrastructure is six independent layers stacked into one engine. Each layer is its own moat."
/>

<BrainHero />

<div className="db-faded-box">
  <div className="db-faded-box__line">The moat is not the bot. <Lime>The moat is the brain.</Lime></div>
</div>

<H2 id="layers">The stack</H2>
<Para>
  Tap any layer to read how it works. The first three feed the brain. The next two decide
  what crosses the threshold from noise into signal. The sixth is what makes the loop closed.
</Para>

<BrainStack />

<H2 id="why">Why six</H2>
<Para>
  A competitor can build a buy bot. A competitor can even build group infrastructure. What a
  competitor cannot replicate is six independently-defensible layers running in convergence,
  backed by the longest-running data record in the category, and gated to a single trading consumer by design.
</Para>
<Para>
  Each layer compounds. New chains, new groups, new alert outcomes. Every new input feeds
  every other layer's accuracy. The brain gets smarter the longer it runs.
</Para>

<TaglineStrip kicker="Longest-running">
  We don't predict. We detect.
</TaglineStrip>
