---
title: "Who builds DeepBot"
sidebar_label: "Who builds DeepBot"
---

{/* Source: pages-rest.jsx -> PageTeam */}

import { PageHead, Para, H2, Lime, Pull, KeyValueGrid } from '@site/src/components/primitives.jsx';
import DeepBot from '@site/src/components/DeepBot.jsx';

<PageHead
  eyebrow="TEAM"
  title={<>Who builds <DeepBot /></>}
  lede={<>Three people. <Lime>Bobby is the longest-running. Dale shipped in six months.</Lime> No VC. The whole platform.</>}
/>

<Para>
  <DeepBot /> is a small team with a long product memory. Krypt, JR, Kraitor. Bobby is the
  longest-running network in the category, self funded, fully shipped. Dale went from concept to live in about six
  months. Fifty plus tools, five chains, full safety stack, AI conversational layer,
  autonomous trading, the social layer scoped, prediction markets next. Built by three
  people, paid for by users, not investors.
</Para>

<Pull>
  Three people built this. Bobby is the longest-running. Dale shipped in six months. The pace is the product.
</Pull>

<div className="db-feat-grid" style={{marginTop: 28}}>
  <div className="db-feat"><div className="db-feat__sym">K</div><div className="db-feat__title">Krypt</div><div className="db-feat__body">Founder. Strategy, voice, product. The decisions that shape what DeepBot is, and what it is not.</div></div>
  <div className="db-feat"><div className="db-feat__sym">J</div><div className="db-feat__title">JR</div><div className="db-feat__body">Engineering. Dale's brain, the tool stack, the integrations.</div></div>
  <div className="db-feat"><div className="db-feat__sym">K</div><div className="db-feat__title">Kraitor</div><div className="db-feat__body">DevOps. Mempool monitoring, the convergence pipeline, the platform that does not go down.</div></div>
</div>

<H2 id="how">How this exists at all</H2>
<Para>
  The longest-running run of Bobby distribution in the category, self funded, no outside money. The network that grew
  in that time is the moat. Six months ago Dale started as a thesis: the same intelligence
  Bobby surfaces to projects could power a conversational trader for the people on the
  other side of the trade. Six months later, Dale is live, conversational, multilingual,
  non custodial, on five chains, with the safety stack of a much larger company.
</Para>

<KeyValueGrid items={[
  { k: "Bobby · longest-running", v: "Built and ran on its own revenue. Group network compounded into the largest project distribution in the space. Every line of the brain was paid for by the brand using it." },
  { k: "Dale · six months", v: "Concept to live. Fifty plus tools wired into a natural language interface. The AI layer is a stack of decisions, not a wrapper around a chat API." },
  { k: "Three people, no VC", v: "Self funded throughout. The team owns the platform. Users pay for what they use. The economics are honest." },
  { k: "Non custodial", v: "Wallets are user owned. Even when Dale executes, DeepBot does not hold the key. The user holds the key, encrypted, but theirs." },
]} />

<Pull>
  <DeepBot /> is non custodial. The user holds the key. Dale never sees it.
</Pull>

<Para>
  Most products at this stage are venture funded with much larger teams and much narrower
  scope. <Lime>The compression is the point.</Lime> Three people who have
  been in the same room for years can build coherent product faster than a team of fifty
  can coordinate one feature.
</Para>
