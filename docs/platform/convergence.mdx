---
title: "Convergence engine"
sidebar_label: "Convergence engine"
---

{/* Source: pages-rest.jsx -> PageConvergence */}

import { PageHead, Para, H2, Lime, Callout } from '@site/src/components/primitives.jsx';

<PageHead
  eyebrow="PLATFORM"
  title="Convergence engine"
  lede="The frame: multi source detection across the communities the network watches, adversarial robust. Why that beats single source signals."
/>

<H2 id="why">A single ping is not an alert</H2>
<Para>
  Most crypto signal products work the same way. One source pings, an alert fires. Spam.
  Manipulation. Noise. The signal to noise ratio is bad on purpose, because volume is what
  gets monetized.
</Para>
<Para>
  Convergence works differently. A single ping does not become an alert.{' '}
  <Lime>Convergence does.</Lime> When intelligence from multiple independent
  sources, weighted for adversarial robustness, agrees that something is happening, and only
  then, an alert fires.
</Para>

<div className="db-faded-box">
  <div className="db-faded-box__line">Watch behavior, not <Lime>theatre.</Lime></div>
  <div className="db-faded-box__close">The principle</div>
</div>

<H2 id="volume">Volume is excluded by design</H2>
<Para>
  Volume is the most manipulable input in crypto. Wash trading, paid pumps, automated
  rotation. The convergence engine does not weight volume into its scoring. Behavior across
  independent layers is what matters. Theatre is what the engine ignores.
</Para>

<H2 id="adversarial">Adversarial robust by construction</H2>
<Para>
  Manipulators can flood a single channel. They cannot flood every channel at once without
  leaving a footprint. The footprint is what the engine looks for. The harder a coordinated
  push tries to look organic, the more visible it becomes to convergence.
</Para>

<H2 id="footprint">The five year footprint</H2>
<Para>
  Cross wallet tracking. Coordinated accumulation detected before it surfaces publicly. Groups
  that no longer exist still contribute to historical pattern matching, because the data
  persists. The active surface is small compared to the footprint, and the footprint is what
  the engine learns from.
</Para>

<H2 id="recipe">12 herbs and spices</H2>
<Callout kind="info" title="We do not reveal the methodology in detail">
  Bobby's value is in being the one place that sees this picture. If we explained how, the
  picture stops being ours. Specifics live in investor and grants surfaces, under NDA where
  warranted, never in public docs.
</Callout>
