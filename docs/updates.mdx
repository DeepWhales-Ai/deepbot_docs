---
title: "Updates this month"
sidebar_label: "Updates this month"
description: "What shipped recently across Dale, the Wallet, Shotgun and the platform."
---

{/* Source: pages-rest.jsx -> PageChangelog. Inline narrative changelog
    (NOT the addendum's auto-generated <Changelog /> reading from JSON;
    this is a hand-written timeline of what shipped). */}

import { PageHead, Lime, StatRow } from '@site/src/components/primitives.jsx';

{/* Aug canon sweep. Refreshed from the stale April window. Every date below
    is taken from a shipped_date in data/feature-status.json rather than
    estimated, so this page and the status data cannot disagree. Shipped items
    only: this is a changelog, not a roadmap. Nothing unreleased goes here. */}

export const ENTRIES = [
  { date: "Aug 10, 2026", tag: "major", title: "DeepBot LINK live in beta", body: "The web surface is open at trade.deepbot.pro. Sign in with Telegram and get native charts, the convergence signal feed, holdings with a dust filter, and a Swap Box on the same wallet and history you already have. Web trading is authorized by your session: there is no PIN on the web surface. It is a beta, and the Telegram surface remains the complete one." },
  { date: "Aug 10, 2026", tag: "major", title: "Auto Buy on Base and BSC", body: "Auto Buy takes screener matches on Base and BSC alongside Ethereum. Same one buy per token rule, same five armed strategy cap. Auto Buy still does not fire on Solana, by design." },
  { date: "Aug 08, 2026", tag: "major", title: "Robinhood Chain live", body: "The sixth chain. Conversational trading, automated exits, and the same 1% EVM trade fee. One private key now works across five EVM chains." },
  { date: "Aug 08, 2026", tag: "major", title: "Token 2022 support live", body: "Token 2022 program assets supported across the screener, trading and transfers, alongside standard SPL. Dale resolves the program for you." },
  { date: "Aug 04, 2026", tag: "major", title: "Prediction markets live", body: "Polymarket and Kalshi through AGG.market. Browse an event, take a position, hold it and close it for cash, all in conversation and funded from your DeepBot wallet." },
  { date: "Aug 01, 2026", tag: "major", title: "Sonar DCA on Solana", body: "Averaging strategies now run on Solana alongside Ethereum, BSC and Base. Buy and sell. Not available on Polygon." },
  { date: "Jul 09, 2026", tag: "major", title: "Voice trading live", body: "Send Dale a voice message and it trades the request, on every account, in English. Dale replies in text. Voice PIN is opt in via /voicepin and off by default." },
  { date: "Jul 01, 2026", tag: "major", title: "Solana exits at parity", body: "Stop loss, trailing stop and tiered take profit on Solana, matching the EVM chains that support exits." },
  { date: "Jul 01, 2026", tag: "major", title: "Token screener and Auto Buy", body: "Describe what you want to buy and Dale watches the live market for it. Auto Buy takes the match without a tap, on Ethereum." },
  { date: "Jul 01, 2026", tag: "major", title: "Solana sends", body: "Native SOL and standard SPL tokens sent to any address from inside Dale, with SNS resolution and precision checked before broadcast." },
  { date: "Jul 01, 2026", tag: "improvement", title: "Convergence board Solana routing", body: "Solana routed through the convergence board alongside the EVM chains." },
  { date: "Jul 01, 2026", tag: "improvement", title: "Correct exit sizing on auto armed buys", body: "When a buy on BSC or Base arms an exit as part of the trade, the exit is sized to the actual quantity bought." },
];

<PageHead
  title="Updates this month"
  lede={<>Everything we shipped between <Lime>July 1 and August 10, 2026</Lime>.</>}
/>

{/* Releases counts ENTRIES so the stat cannot drift from the list below it.
    The old "Deployments: 14" stat is gone: it was tied to the April window
    and there is nothing in this repo to recompute it from. A stat we cannot
    prove is worse than one we do not show. */}
<StatRow items={[
  { num: String(ENTRIES.length), label: "Releases" },
  { num: "6",  label: "Chains live" },
  { num: "1%", label: "Support load" },
]} />

<div className="db-changelog">
  {ENTRIES.map((e, i) => (
    <div className="db-changelog__entry" key={i}>
      <div className="db-changelog__rail">
        <div className="db-changelog__dot" />
        {i < ENTRIES.length - 1 && <div className="db-changelog__line" />}
      </div>
      <div className="db-changelog__main">
        <div className="db-changelog__meta">
          <span className="db-changelog__date">{e.date}</span>
          <span className={`db-changelog__tag db-changelog__tag--${e.tag}`}>{e.tag}</span>
        </div>
        <div className="db-changelog__title">{e.title}</div>
        <div className="db-changelog__body">{e.body}</div>
      </div>
    </div>
  ))}
</div>
